using UnityEngine;

namespace TARDIS.Main
{
    /// <summary>
    /// Manages the personality aspects of the TARDIS, including interactions and responses.
    /// </summary>

    public class _42Personality : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}